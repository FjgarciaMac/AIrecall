storage:
  backend: sqlite
  path: "./airecall.db"

retrieval:
  top_k: 5
  mode: hybrid          # keyword | vector | hybrid

summarization:
  enabled: true
  run_every_hours: 24
  compact_after_days: 30
// draft note 1405
